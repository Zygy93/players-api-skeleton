const User = require('./user'); // This pulls the variable User from the user index in the models
const Player = require('./player'); // This pulls the variable User from the user index in the models
module.exports = {
  User,
  Player
};
//Exports the User and Player API